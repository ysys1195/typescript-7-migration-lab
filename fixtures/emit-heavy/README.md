# emit-heavy

Generated implementation files emphasize JavaScript emit. The runner removes
the output directory before every timed invocation. Parsing and type checking
remain part of the compiler invocation.
