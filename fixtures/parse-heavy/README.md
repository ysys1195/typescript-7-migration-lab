# parse-heavy

Large generated syntax trees with `noCheck` and `noEmit` emphasize parsing and
program construction. The measurement still includes process startup, config
loading, binding, and filesystem work; it is not a parser-only microbenchmark.
